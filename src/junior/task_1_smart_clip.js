/**
 * Provide a function that
 * - prints the numbers from 1 to 100
 * - AND on every multiple of 3, print the term smart
 * - AND on every multiple of 5 print the term clip.
 *
 * Note: Print with console.log, print terms like <number> <term> e.g. 3 smart
 */

export const printSmartClip = () => {

}
