/**
 * Modify the Counter component
 *
 * - add required props: countPrefix of type string
 * - the component should hold a state for the current count
 *
 * - it renders a h1 headline containing countPrefix and current count
 * - it renders a button with the text: Increase Count
 * - when the button is clicked, the count is increased and displayed to the user
 *
 * Notes:
 * - The styling is not relevant in this task.
 * - You could run the app with npm run start to see your component if you need to.
 */

export const Counter = () => {
    return <div>Counter</div>
}